import { Routes } from '@angular/router';
import { BasicButtonComponent } from './components/buttons/basic-button';

export const routes: Routes = [
  {
    path: 'buttons/basic',
    component: BasicButtonComponent,
  },
];
